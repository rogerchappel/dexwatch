# dexwatch examples

These examples use only checked-in fixtures. They do not call DexScreener or any
other network service.

```sh
bash examples/filter-ethereum.sh
```

Generated files are written under `examples/out/` and ignored by Git.
